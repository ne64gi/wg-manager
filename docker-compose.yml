services:
  wireguard:
    image: lscr.io/linuxserver/wireguard:latest
    container_name: wg-studio-wireguard
    cap_add:
      - NET_ADMIN
      - SYS_MODULE
    environment:
      - PUID=1000
      - PGID=1000
      - TZ=Asia/Tokyo
    volumes:
      - ./config:/config
      - ./data:/data
      - /lib/modules:/lib/modules:ro
    ports:
      - "51820:51820/udp"
    sysctls:
      - net.ipv4.ip_forward=1
      - net.ipv4.conf.all.src_valid_mark=1
    restart: unless-stopped

  wg-studio-api:
    build:
      context: .
    container_name: wg-studio-api
    working_dir: /app
    command: ["uvicorn", "app.api.main:app", "--host", "0.0.0.0", "--port", "8000", "--reload"]
    environment:
      - DATABASE_URL=sqlite:////data/wg-studio.db
      - LOG_DATABASE_URL=sqlite:////data/wg-studio-log.db
      - ARTIFACT_ROOT=/wg/config
      - WG_CONTAINER_NAME=wg-studio-wireguard
      - WG_INTERFACE_NAME=wg0
      - WG_CONTAINER_CONFIG_PATH=/config/wg_confs/wg0.conf
    volumes:
      - ./app:/app/app
      - ./data:/data
      - ./config:/wg/config
      - /var/run/docker.sock:/var/run/docker.sock
    ports:
      - "8000:8000"

  wg-studio-cli:
    build:
      context: .
    container_name: wg-studio-cli
    working_dir: /app
    entrypoint: ["python", "-m", "app.cli.main"]
    environment:
      - DATABASE_URL=sqlite:////data/wg-studio.db
      - LOG_DATABASE_URL=sqlite:////data/wg-studio-log.db
      - ARTIFACT_ROOT=/wg/config
      - WG_STUDIO_API_URL=http://wg-studio-api:8000
    volumes:
      - ./app:/app/app
      - ./data:/data
      - ./config:/wg/config
      - ./tests:/app/tests
